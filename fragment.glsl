#version 330 core
out vec4 FragColor;
// uniform vec4 transformColor;

void main()
{
    //vec2 circle = vec2(1.0f, 1.0f);
    //vec2 uv = fragCoord.xy;
    //vec2 resolution = vec2(1920.0f, 1080.0f);
    //vec2 center = resolution * 0.5;
    //float radius = resolution.y * 0.25;
    //float distance = length(center, uv);
    //float c = clamp(distance, 0, 1);
    // FragColor = vec4(c, c, c, 1.0f);
    FragColor = vec4(1.0f, 0.0f, 0.0f, 1.0f);
}